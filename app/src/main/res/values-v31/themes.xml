<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="postSplashScreenTheme">@style/Theme.ApplyDigitalChallenge</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/ad_blue</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/animated_logo</item>
        <!-- Required for animated icons. -->
        <item name="windowSplashScreenAnimationDuration">200</item>
    </style>
</resources>